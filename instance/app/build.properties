name=hsearch-teaser
version=0.00a

company=Bizosys Technologies Pvt Ltd.

basedir = ../../
src.dir = ${basedir}/src/java
lib.dir = ${basedir}/lib
conf.dir = ${basedir}/conf
plugins.dir = ${basedir}/src/plugin
docs.dir = ${basedir}/docs
web.src.dir = ${basedir}/src/web
js.src.dir = ${basedir}/src/web/static/js
css.src.dir = ${basedir}/src/web/core/css

build.dir = ${basedir}/build
build.classes = ${build.dir}/classes
build.webapps = ${build.dir}/webapps
build.plugins = ${build.dir}/plugins
build.docs = ${build.dir}/docs
build.javadoc = ${build.docs}/api
build.encoding = ISO-8859-1
build.web.dir = ${build.dir}/web

test.src.dir = ${basedir}/src/test
test.build.dir = ${build.dir}/test
test.build.data =  ${test.build.dir}/data
test.build.classes = ${test.build.dir}/classes
test.build.javadoc = ${test.build.dir}/docs/api

context.root = ${name}
web.src.dir = ${basedir}/src/web
src.webapps = ${basedir}/src/webapps
HTTP_HOME=f:/install/servers/Tomcat 6.0/webapps

dist.dir=${build.dir}/${name}

javac.debug=on
javac.optimize=on
javac.deprecation=off
javac.version=1.6